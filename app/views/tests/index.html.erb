<p id="notice"><%= notice %></p>

<h1>Tests</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Questions</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>

    <% @tests.each do |test| %>
      <tr>
        <td><%= test.name %></td>
        <td><%= test.description %></td>
        <td><%= test.questions.count %></td>
        <td><%= link_to 'Show', test %></td>
        <td><%= link_to 'Edit', edit_test_path(test) %></td>
        <td><%= link_to 'Destroy', test, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <%# <td><%= link_to 'Destroy', test, method: :deleteTest, data: { confirm: 'Are you sure?' } % ></td> %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
      <%= @tests.inspect %>

<%= link_to 'New Test', new_test_path %>
