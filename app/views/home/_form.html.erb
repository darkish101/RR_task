<%# <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |user| % > %>
<%# <%= form_for(resource, as: resource_name, url: new_home_path(resource_name)) do |user| % > %>
<%= form_with(model: user, url: home_index_path(resource_name)) do |user| %>

<%# <%= form_for(resource, as: resource_name, url: home_index_path(resource_name)) do |user| % > %>
<%# <%= form_for(@user) do |user| % > %>
<%# <%= form_for @user, url: home_index_path(@user)  do |user| % >  %>
<%# <%= form_for(@user) do |user| % > %>
  <% if resource.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(resource.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
        <% resource.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= user.label :email %><br />
    <%= user.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div class="field">
    <%= user.label :name %><br />
    <%= user.text_field :name, autofocus: true, autocomplete: "name" %>
  </div>
  <div class="field">
    <%= user.label :role %><br />
    <%# <%= user.text_field :role, autofocus: true, autocomplete: "role" % >  %>
    <%# <%= f.select(:player_id, Player.all.pluck(:name, :id), @score.player_id % > %>
    <%# <%= user.select :role, @roles, :prompt => 'Select One' % > %>
    <%# <%= user.select(:role, @roles, :prompt => 'Select') % > %>
    <%= user.select :role, ["teacher", "student"], :prompt => 'Select One' %>

  </div>




  <div class="field">
    <%= user.label :password %>
    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %> characters minimum)</em>
    <% end %><br />
    <%= user.password_field :password, autocomplete: "new-password" %>
  </div>

  <div class="field">
    <%= user.label :password_confirmation %><br />
    <%= user.password_field :password_confirmation, autocomplete: "new-password" %>
  </div>

  <div class="actions">
    <%= user.submit "Sign up" %>
  </div>
<% end %>


<%# 
  <div class="actions">
    <%= form.submit %s>
  </div>
<% end %s> %>
